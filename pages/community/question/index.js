import QuestionComp from "../../../components/community/QuestionComp";

const Question = () => {
  return (
    <React.Fragment>
      <QuestionComp isForUpdate={false} />
    </React.Fragment>
  );
};

export default Question;
